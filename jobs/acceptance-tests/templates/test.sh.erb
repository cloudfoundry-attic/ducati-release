#!/bin/bash


export GOROOT=$(readlink -nf /var/vcap/packages/golang)

cd /var/vcap/packages/ducati
export GOPATH=$PWD
export PATH=$GOROOT/bin:$GOPATH/bin:$PATH

export GARDEN_SERVER="<%= p('ducati.acceptance_tests.garden_server') %>"

echo "targeting $GARDEN_SERVER"
cd src/acceptance
ginkgo -r
